import Layout from "../../components/Layout";
import styles from "./index.module.scss";

const Activities = (props) => (
  <Layout title="Activities" className={styles.activities} {...props}>
    Activities
  </Layout>
);

export default Activities;
